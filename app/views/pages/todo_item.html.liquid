---
slug: todo_items
---
{%- assign id = context.params.slug2 -%}
{%- graphql g = "get_todos", id: id -%}

{%- if id == blank -%}
  {% include_form 'todo_item' %}
  {% include 'todo_item/list', todos: g.models.results %}
{%- else -%}
  {% include 'todo_item/show', todo: g.models.results.first, id: id %}
{%- endif -%}