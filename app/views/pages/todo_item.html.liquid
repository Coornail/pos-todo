---
slug: todo_items
---
{%- assign id = todo_item.params.slug2 -%}
{%- graphql g = "get_todos", id: id -%}

{%- if id == blank -%}
  {% include_form 'todo_item' %}
  {% include 'todo_item/list', todos: g.models.results %}
{%- else -%}
  {% include 'todo_item/show', todos: g.models.results.first %}
{%- endif -%}